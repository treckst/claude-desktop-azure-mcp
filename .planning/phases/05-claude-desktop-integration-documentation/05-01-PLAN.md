---
phase: 5
plan: 1
name: Claude Desktop Turnkey Configuration
wave: 1
depends_on: []
files_modified:
  - claude_desktop_config.example.json
requirements:
  - R5.1
---

# Plan 5-01: Claude Desktop Turnkey Configuration

## Objective
Provide a production-ready example configuration file `claude_desktop_config.example.json` demonstrating how to connect the `AzureTelemetryBridge` MCP server to Claude Desktop via the `stdio` transport across operating systems.

## Code Patterns (Model Resilience)
Conventions:
- JSON structure conforms to Anthropic Claude Desktop MCP configuration standards.
- Clearly demonstrates `command` (Python executable), `args` (path to `server.py`), and environment variable bindings (`AZURE_STORAGE_ACCOUNT_NAME`, `AZURE_CONTAINER_NAME`, `AZURE_BLOB_NAME`).

## Tasks

<task type="auto">
  <name>Finalize Claude Desktop Configuration Example</name>
  <files>claude_desktop_config.example.json</files>
  <action>
    1. Ensure `claude_desktop_config.example.json` contains valid JSON with `mcpServers` object.
    2. Define `azure-telemetry-bridge` server with `command`, `args`, and `env`.
  </action>
  <verify>
    powershell -Command "Get-Content claude_desktop_config.example.json | ConvertFrom-Json"
  </verify>
  <done>
    `claude_desktop_config.example.json` exists and parses cleanly as valid JSON.
  </done>
</task>

## Must-Haves
- Valid JSON syntax.
- Accurate environment variable names.
