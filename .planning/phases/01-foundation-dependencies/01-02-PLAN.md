---
phase: 1
plan: 2
name: Pinned Python Dependency Specification
wave: 2
depends_on:
  - 1
files_modified:
  - requirements.txt
requirements:
  - R1.1
---

# Plan 1-02: Pinned Python Dependency Specification

## Objective
Define pinned, verified dependencies in `requirements.txt` to support the FastMCP server, Azure authentication, Azure Blob storage streaming, tabular telemetry analysis with Pandas, Pytest test mocking, and Ruff linting, strictly retaining compatibility with the FastMCP v1 API interface.

## Code Patterns (Model Resilience)
Conventions:
- Group dependencies by architectural tier: MCP Server Framework, Azure Cloud SDK, Data Processing, and Development & Quality.
- Include explanatory comments for version constraints (specifically noting `mcp[cli]<2.0.0` for `FastMCP` decorator compatibility).
- Ensure strict compliance with Python 3.10+ / 3.11 runtimes.

## Tasks

<task type="auto">
  <name>Create requirements.txt with Pinned Dependencies</name>
  <files>requirements.txt</files>
  <action>
    1. Create `requirements.txt` at repository root.
    2. Add Core MCP Protocol & CLI:
       - `mcp[cli]>=1.3.0,<2.0.0` with comment noting preservation of `from mcp.server.fastmcp import FastMCP`.
    3. Add Azure Cloud Authentication and Storage SDK:
       - `azure-identity>=1.15.0,<=1.26.0` (for `DefaultAzureCredential`)
       - `azure-storage-blob>=12.19.0,<=12.31.0` (for `BlobServiceClient` and BlobClient operations)
    4. Add Data Processing:
       - `pandas>=2.0.0,<3.1.0` (for tabular CSV parsing, DataFrame filtering, and metric aggregations)
    5. Add Development & Quality Tools:
       - `pytest>=8.0.0` (for test suite execution and mock verification)
       - `ruff>=0.4.0` (for code formatting and lint verification)
    6. Save file with UTF-8 encoding and standard Unix/Windows line endings.
  </action>
  <verify>
    python -c "lines = [l.strip() for l in open('requirements.txt', encoding='utf-8') if l.strip() and not l.startswith('#')]; print('Package count:', len(lines)); assert len(lines) == 6, f'Expected 6 dependencies, found {len(lines)}'"
  </verify>
  <done>
    `requirements.txt` is created with all 6 required dependencies pinned with verified upper/lower bounds.
  </done>
</task>

## Must-Haves
- `requirements.txt` exists at repository root.
- Contains `mcp[cli]`, `azure-identity`, `azure-storage-blob`, `pandas`, `pytest`, and `ruff`.
- `mcp[cli]` includes `<2.0.0` upper bound to prevent breaking changes to FastMCP imports.
- Verification command passes with exactly 6 active dependency specifications.

---
*Created: 2026-09-07*
