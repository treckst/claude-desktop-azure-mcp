---
phase: 4
plan: 1
name: Offline Pytest Test Suite and Mocks
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/test_server.py
requirements:
  - R4.1
---

# Plan 4-01: Offline Pytest Test Suite and Mocks

## Objective
Deliver a comprehensive unit and integration test suite in `tests/test_server.py` using `unittest.mock` to ensure offline resilience, zero-cloud dependency, and 100% coverage of server fallback, telemetry queries, and TCO calculations.

## Code Patterns (Model Resilience)
Conventions:
- Group tests into clear test classes: `TestDataLoadingAndFallback`, `TestGetClientTelemetryTool`, `TestCalculateModernizationTcoTool`, `TestFastMCPRegistration`.
- Mock external Azure SDK classes via `server.BlobServiceClient` and `server.DefaultAzureCredential`.
- Validate error handling and math accuracy down to cents and percentages.

## Tasks

<task type="auto">
  <name>Implement Pytest Test Suite</name>
  <files>tests/test_server.py</files>
  <action>
    1. Implement test cases in `tests/test_server.py`.
    2. Run `pytest -v tests/` to verify all 14 tests pass.
  </action>
  <verify>
    powershell -Command ".\.venv\Scripts\pytest -v tests/"
  </verify>
  <done>
    14 unit and integration tests pass with zero failures.
  </done>
</task>

## Must-Haves
- 100% offline test execution without Azure credentials.
- Test coverage for mock fallback, telemetry queries, and TCO math.
